/*
Aufgabe 7
Erstellen Sie ein Programm zum Lesen eines Strings. Das Programm gibt am Ende aus, wie
viele Zeichen, Zeilen und Worte gezählt wurden. Das Programm läuft solange weiter bis der
Benutzer das Programm beendet.
Hinweise: Als Zeilenanzahl kann man die Anzahl der ‚\n‘ verwenden. Um die Anzahl der Worte
festzustellen, muss man sich merken, ob man gerade in einem Wort oder außerhalb eines
Wortes ist und dann, wenn man diesen Zustand von „im-Wort“ auf „außerhalb-Wort“ wechselt,
die Anzahl der Worte erhöhen.
Hilfe: inkludieren Sie ctype.h, um die Funktion isspace zu verwenden.
Verwenden Sie dazu
While ((c = getchar()) != EOF)
*/

#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

int main()
{
    printf("Test");
    return 0;
}